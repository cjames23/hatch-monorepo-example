"""Top-level `my_app` package.

Defined by the repository-root `pyproject.toml`. It is the workspace *root*
(it declares the workspace environments) and also a real, installable
application that consumes the workspace members.
"""

__version__ = "0.1.0"
